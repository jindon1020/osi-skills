Return one complete JSON object for the request in state.

If state contains `validation_error` or `retry_instruction`, correct the full response and regenerate the complete object. Do not continue or patch the previous output. Return JSON only, without Markdown fences.
